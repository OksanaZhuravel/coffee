import { Inter } from 'next/font/google'

export const inter = Inter({
	weight: ['600'],
	subsets: ['latin'],
	display: 'swap',
})

export const interRegular = Inter({
	weight: ['400'],
	subsets: ['latin'],
	display: 'swap',
})
